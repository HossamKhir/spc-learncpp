#include <iostream>

using namespace std;
// ^ -> `using` directive allows for accessing identifiers in namespaces w/o
// namespace prefix

int main(void)
{
    cout << "Hello, World!";
    return 0;
}